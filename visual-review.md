# تقرير المراجعة البصرية والتقنية — الجولة الجديدة

## نطاق الإصلاح

تمت معالجة خلل التفاف الخط الزمني على الهاتف عبر إعادة ضبط أعمدة الشبكة، وتثبيت عرض عمود المحتوى، والسماح بالالتفاف الطبيعي بين الكلمات بدلاً من تكسير الكلمة العربية حرفاً حرفاً. أضيفت كذلك بطاقات مؤلفات قابلة للتوسّع، مع حالة واضحة تميّز بين الفهرس العام المتاح وبين العنوان الذي لا يملك ملفاً عاماً مرفقاً.

أضيف معرض أرشيفي منظم بأربع صور أجواء بحثية: مخطوط، مكتب بحث، قاعة محاضرة، ورفوف مكتبة. الصور ليست صوراً شخصية للدكتور ولا تُستخدم لإيهام الزائر بأنها أحداث حقيقية؛ وقد أُضيف تنبيه صريح بذلك داخل الموقع.

## الاختبارات التقنية

| الفحص | النتيجة |
|---|---|
| `pnpm check` | ناجح، دون أخطاء TypeScript |
| `pnpm test` | ناجح: 5 ملفات اختبار و7 حالات |
| `pnpm build` | ناجح، تم إنتاج نسخة الإنتاج |
| خادم التطوير | يعمل بعد إعادة التشغيل، OAuth وTypeScript دون أخطاء |

## سلامة الملفات والروابط

تم فحص كل الأصول المستخدمة في الجولة عبر الخادم المحلي مع اتباع إعادة التوجيه حتى المصدر النهائي. ملفات PDF العربية والإنجليزية أعادت الحالة 200 ونوع `application/pdf`. أصول المعرض الأربعة أعادت الحالة 200 وأنواع صور `image/webp` بأحجام غير صفرية.

| الأصل | الحالة | النوع |
|---|---:|---|
| PDF العربية | 200 | application/pdf |
| PDF الإنجليزية | 200 | application/pdf |
| صورة المخطوط | 200 | image/webp |
| صورة مكتب البحث | 200 | image/webp |
| صورة قاعة المحاضرات | 200 | image/webp |
| صورة المكتبة | 200 | image/webp |

## روابط المؤلفات

تمت إضافة رابط فهرس عام موثوق لعنوان «شعر الجهاد والحرب في عهد بني الأحمر»، وإضافة رابط ملف المؤلف العام في قاعدة أفيقة. بقية العناوين تعرض حالة «لا يوجد ملف عام مرفق» مع زر لطلب نسخة عبر البريد؛ لم يتم إنشاء ملفات وهمية أو نسب ملفات غير متاحة إلى المؤلفات.

المصادر العامة المستخدمة:

- https://afiqa.net/authors/details/25960
- https://kiqp.net/search?keyword=شعر الجهاد والحرب في عهد بني الاحمر

## المراجعة البصرية

تم التقاط مراجعة كاملة للصفحة على شاشة مكتبية بعرض 1280px، ومراجعة كاملة على شاشة هاتف بعرض 390px. ظهرت بنية المعرض والبطاقات والـPDF داخل التخطيط، مع بقاء عناصر التحكم قابلة للاستخدام في النسخة الضيقة. أُعيد تشغيل الخادم قبل المراجعة للتأكد من عدم بقاء خطأ `dotenv` القديم.

## جولة إزالة رقم 07 وإصلاح الهاتف

تمت إزالة رقم 07 من عنوان المعرض الأرشيفي، وأصبح ترقيم الأقسام اللاحقة متسقاً بصرياً. أُعيد بناء محطة الخط الزمني على العرض الضيق باستخدام أعمدة مرنة: عقدة مستقلة، عمود نص كامل العرض، وزر توسعة منفصل. تم التقاط مراجعة مكتبية نهائية بعرض 1280px بعد هذا التعديل، إلى جانب مراجعة الهاتف بعرض 390px. نجح `pnpm check` و`pnpm test` بنتيجة 7 حالات ناجحة و`pnpm build`.

## حركة التمرير في الخط الزمني

أضيف مراقب ظهور يعتمد على `IntersectionObserver`؛ تظهر كل محطة عند دخولها مجال الرؤية بحركة خفيفة إلى الأعلى مع تدرج زمني بسيط، وتظهر العقدة بتكبير هادئ، بينما تبقى حركة فتح التفاصيل مستقلة وخفيفة. عند غياب `IntersectionObserver` تظهر المحطات مباشرة كحل احتياطي. كما تم تعطيل الحركة غير الضرورية تلقائياً عند تفعيل `prefers-reduced-motion`. تمت مراجعة العرض على الكمبيوتر والهاتف، ونجحت `pnpm check` و`pnpm test` و`pnpm build`.

## إزالة المعرض وإضافة الانتقالات الناعمة

تم حذف قسم المعرض الأرشيفي من الصفحة الرئيسية والتنقل، وأعيد ترقيم قسم اللجان إلى 07 والتكريمات إلى 08. أضيفت انتقالات ظهور ناعمة للأقسام عند دخولها الشاشة، مع انتقالات قصيرة للأزرار والبطاقات وعناصر التفاعل، واستمرار حركة الخط الزمني السابقة. تمت مراجعة الصفحة كاملة على الكمبيوتر بعرض 1280px والهاتف بعرض 390px؛ بقيت الأقسام متصلة دون فراغات أو عناصر معرض متبقية، وظل التنقل والـPDF والخط الزمني قابلين للاستخدام. تم اختبار `prefers-reduced-motion` عبر قواعد CSS و`scrollIntoView` باختيار حركة تلقائية عند تفعيل تقليل الحركة.

## تحقق تشغيل الخادم بعد إزالة المعرض

كان سجل قديم يحتوي على `ERR_MODULE_NOT_FOUND` لحزمة `dotenv`. بعد التأكد من وجود الحزمة وإعادة تشغيل خادم التطوير، ظهر في السجل الجديد: `[OAuth] Initialized with baseURL: https://api.manus.im` ثم `Server running on http://localhost:3000/` دون ظهور خطأ dotenv جديد. أُعيد تشغيل `pnpm check && pnpm test && pnpm build` بعد إعادة التشغيل، ونجحت الفحوص: 5 ملفات اختبار و7 حالات ناجحة وبناء الإنتاج مكتمل.

## شريط تقدم القراءة وانتقالات اللغة والثيم

أضيف شريط تقدم ثابت أعلى الصفحة يحسب نسبة التمرير من طول المستند ويعرضها عبر `role=progressbar` مع قيمة وصول من 0 إلى 100. كما أضيفت حالات انتقال خاصة عند تبديل العربية والإنجليزية وعند التحويل بين الوضع الليلي والنهاري، تتضمن تلاشيًا خفيفًا ولمعة قصيرة على زر التبديل دون تعطيل التفاعل. تمت مراجعة العرض الكامل على الكمبيوتر والهاتف، ونجحت اختبارات الواجهة الجديدة التي تتحقق من ظهور شريط القراءة وتفعيل حالتي الانتقال. الجولة البرمجية الأخيرة نجحت: 5 ملفات اختبار، 8 حالات، و`pnpm check` و`pnpm build` مكتملان.

## إصلاح فتح محطات الخط الزمني على الهاتف

سبب الخلل كان أن قاعدة `display: block` الخاصة بتفاصيل المحطة على الهاتف كانت تتغلب على خاصية `hidden`، فتظل المساحات محجوزة حتى للمحطات المغلقة. أضيفت قاعدة صريحة لـ`[hidden]` تمنع حجز أي مساحة، مع تثبيت ارتفاع المحطة المفتوحة على `auto` وإبقاء التفاصيل مباشرة تحت محتوى المحطة. نجحت اختبارات فتح وإغلاق المحطة، وفحص TypeScript والبناء، وتمت مراجعة عرض الهاتف بعد الإصلاح دون فراغات غير مبررة في قسم المسيرة.

تمت إضافة مراجعة مكتبية جديدة بعد إصلاح المحطات. عرض الصفحة الكامل على الكمبيوتر بقي مستقراً، ولم يظهر أي تغيير غير مرغوب في تخطيط المسيرة أو الأقسام الأخرى. اختبار DOM لفتح وإغلاق المحطة يثبت أن التفاصيل تظهر وتختفي فعلياً، بينما تمنع قاعدة `[hidden]` بقاء المساحات المحجوزة للمحطات المغلقة.

## إعادة بناء الخط الزمني بعد تكرار الخلل

أُزيل الاعتماد على خاصية `hidden` في تفاصيل محطات الخط الزمني، وأصبحت التفاصيل تُصيّر شرطياً فقط للمحطة المفتوحة. كما أعيد بناء تخطيط الهاتف بصف مستقل لكل محطة، مع وضع التفاصيل في العمود الثاني داخل البطاقة نفسها؛ بذلك لا يمكن للمحطات المغلقة حجز ارتفاع، ولا تنتقل العقدة إلى طرف غير مقصود. أضيف اختبار واجهة يفتح المحطة الثانية ويتحقق من وجود كتلة تفاصيل واحدة مباشرة بعدها. نجحت الجولة: 5 ملفات اختبار و9 حالات، و`pnpm check` و`pnpm build`، مع مراجعة بصرية جديدة للهاتف والكمبيوتر.

## تحقق شامل من جميع محطات المسيرة

أضيف اختبار واجهة يمر على المحطات الست واحدة تلو الأخرى، ويفتح كل محطة، ويتحقق من أن كتلة تفاصيل واحدة فقط موجودة مباشرة بعد زر المحطة نفسها. نجح الاختبار مع جميع المحطات. كما تمت مراجعة نسخة الهاتف بعد إعادة البناء؛ بقي الخط الزمني ضمن عرض الصفحة ولم تظهر المسافات الناتجة عن التفاصيل المغلقة. المراجعة البصرية العامة لا تغني عن الاختبار التفاعلي، لذلك تم توثيق الاختبار التتابعي داخل `Home.ui.test.tsx`.

تمت مراجعة حالة محطة متأخرة بصرياً على الهاتف بتفعيل المحطة الخامسة مؤقتاً أثناء المعاينة، ثم أُعيد السلوك الافتراضي إلى فتح المحطة الأولى للزائر. في الحالة المتأخرة، بقيت بنية البطاقة داخل عرض الصفحة ولم تُنشئ التفاصيل مساحة منفصلة أو انزياحاً للعقدة؛ كما تحقق اختبار الواجهة من انتقال الفتح بين جميع المحطات الست. أُعيد تشغيل الفحص والبناء بعد إرجاع الحالة الافتراضية ونجحت 5 ملفات اختبار و10 حالات.

## مراجعة هاتفية مخصصة لمحطة متأخرة مفتوحة

لإعادة إنتاج شكوى المستخدم بصرياً، فُتحت المحطة الخامسة مؤقتاً كحالة البداية في معاينة الهاتف ثم التُقطت لقطة مخصصة لهذه الحالة، وبعدها أُعيدت الحالة الافتراضية إلى المحطة الأولى. النتيجة الموثقة: بطاقة المحطة المتأخرة تبقى ضمن تدفق الخط الزمني، وتأتي كتلة الملاحظة بعدها مباشرة، ولا تُنشئ كتلاً مخفية أو مساحات محجوزة للمحطات الأخرى. يدعم ذلك اختبار viewport المخصص بعرض 390px واختبار المرور على المحطات الست.

تم تقوية إثبات المحطة المتأخرة برمجياً: عند ضبط عرض الهاتف إلى 390px وفتح المحطة الخامسة، يتحقق الاختبار من أن التفاصيل الوحيدة الموجودة هي كتلة واحدة مباشرة بعد زر المحطة المفتوحة، وأنه لا توجد عناصر `.timeline-detail[hidden]` أو كتل تفاصيل إضافية داخل المحطة. نجحت هذه الحالة مع بقية الاختبارات، ليصبح الإجمالي 5 ملفات اختبار و11 حالة، مع نجاح TypeScript والبناء.

## الجولة النهائية: إزالة القانون الدولي، البحث، التباين، وملفات PDF

تم تنظيف مصدر البيانات العربي والإنجليزي ومولد PDF من كل ذكر للمصطلح المحذوف، واستبداله بالنقد الأدبي والأدب الأندلسي. أُعيد توليد ملفي PDF ورفعهما إلى التخزين، ثم ثُبّتت الروابط الجديدة داخل الصفحة: `/manus-storage/raad-nasser-cv-ar_ba7e77d5.pdf` و`/manus-storage/raad-nasser-cv-en_d9d7f493.pdf`.

أضيف بحث داخلي قابل للفتح والإغلاق من الترويسة، ويبحث في الأقسام والمناصب والدورات والمؤلفات، مع نتائج قابلة للنقر تعيد الزائر إلى القسم المقصود. كما تم التحقق من أزرار التنزيل واللغة والثيم والقائمة بعناوين وصول واضحة.

في المراجعة البصرية الأخيرة، تم فحص لقطة سطح المكتب الفاتح 1280×720، ولقطة سطح المكتب الداكن 1280×720، ولقطة الهاتف الداكن 390×844، ولقطة لوحة البحث المفتوحة في الوضع الداكن على سطح المكتب. زر الاستكشاف الرئيسي وزر التواصل يظهران الآن بخلفيات ونصوص متباينة؛ أزرار الترويسة ولوحة البحث والنتائج واضحة، ولا يظهر تداخل أو انكسار في تخطيط الهاتف.

| الفحص الأخير | النتيجة |
|---|---|
| `pnpm check` | ناجح دون أخطاء TypeScript |
| `pnpm test` | ناجح: 5 ملفات اختبار و14 حالة |
| `pnpm build` | ناجح، تم إنتاج نسخة الإنتاج |
| تنظيف المصطلح المحذوف | ناجح في `client/src` و`create_cv_pdfs.py`، مع اختبارات منع الرجوع |
| PDF العربي والإنجليزي | أُعيد توليدهما ورفعهما وتحديث الروابط |
| light/dark desktop | تمت المراجعة البصرية |
| dark mobile | تمت المراجعة البصرية عند 390px |
| لوحة البحث الداكنة | تمت المراجعة البصرية مع النتائج الست الأولية |

استكمال التغطية اللونية: تمت مراجعة لقطة الهاتف الفاتح بعرض 390px، وظهرت أزرار القائمة وPDF والثيم واللغة والبحث واضحة دون تداخل، مع بقاء زرَي التواصل والاستكشاف متباينين. كما تمت مراجعة لوحة البحث المفتوحة في الوضع الفاتح على سطح المكتب، وظهرت خانة البحث وزر الإغلاق والنتائج الست الأولية بحدود وعناوين قابلة للقراءة. بذلك أصبحت المراجعة موثقة للحالتين الفاتحة والداكنة على الهاتف وسطح المكتب، مع لوحة البحث في الوضعين.

## تحديث صياغة المؤهل الدراسي

تم استبدال «الدكتوراه الأولى» بـ«الدكتوراه» في مصدر بيانات الموقع ومولد PDF، واستبدال `First Ph.D.` بـ`Ph.D.` في النسخة الإنجليزية. أُعيد توليد ملفي PDF ورفعهما بروابط جديدة، وأظهر استخراج النص من الملفين عدم وجود الصياغة القديمة. نجحت الفحوص البرمجية: TypeScript، 14 اختباراً، وبناء الإنتاج.
